{
  "name": "kfc-coupon-solver",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "點兩下（想吃什麼 + 價格帶）, 列出該價格帶裡最便宜的 3 張肯德基優惠券",
  "scripts": {
    "test": "vitest run",
    "serve": "py -3.10 -m http.server 8731 --bind 0.0.0.0 --directory web",
    "refresh": "py -3.10 -u scripts/gather.py && py -3.10 -u scripts/build_taxonomy.py"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
